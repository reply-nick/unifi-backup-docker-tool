services:
  unifi-backup-docker-tool:
    container_name: unifi-backup-docker-tool
    build: .
    env_file: .env
    environment:
      - TZ=America/Chicago
    volumes:
      - ./backups:/backups
    restart: unless-stopped
